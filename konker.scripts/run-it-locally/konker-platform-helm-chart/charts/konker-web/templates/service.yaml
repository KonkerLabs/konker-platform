apiVersion: v1
kind: Service
metadata:
  name: {{ include "konker-web.fullname" . }}
  labels:
    {{- include "konker-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "konker-web.selectorLabels" . | nindent 4 }}
