apiVersion: v1
kind: Service
metadata:
  name: {{ include "konker-router.fullname" . }}
  labels:
    {{- include "konker-router.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "konker-router.selectorLabels" . | nindent 4 }}
