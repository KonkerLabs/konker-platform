apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "konker-api.fullname" . }}-test-connection"
  labels:
    {{- include "konker-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "konker-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
