apiVersion: v1
kind: Service
metadata:
  name: {{ include "konker-api.fullname" . }}
  labels:
    {{- include "konker-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "konker-api.selectorLabels" . | nindent 4 }}
