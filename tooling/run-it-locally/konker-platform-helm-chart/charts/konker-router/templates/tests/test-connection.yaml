apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "konker-router.fullname" . }}-test-connection"
  labels:
    {{- include "konker-router.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "konker-router.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
