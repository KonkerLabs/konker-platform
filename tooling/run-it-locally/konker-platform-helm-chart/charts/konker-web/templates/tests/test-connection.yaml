apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "konker-web.fullname" . }}-test-connection"
  labels:
    {{- include "konker-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "konker-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
