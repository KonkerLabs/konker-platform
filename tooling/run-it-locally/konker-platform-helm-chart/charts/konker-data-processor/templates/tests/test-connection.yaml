apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "konker-data-processor.fullname" . }}-test-connection"
  labels:
    {{- include "konker-data-processor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "konker-data-processor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
