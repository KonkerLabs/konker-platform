apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "konker-registry-data-helm.fullname" . }}
  labels:
    app: {{ include "konker-registry-data-helm.name" . }}
    chart: {{ include "konker-registry-data-helm.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    project: registry
spec:
  selector:
    matchLabels:
      app: {{ include "konker-registry-data-helm.name" . }}
  endpoints:
  - port: web
